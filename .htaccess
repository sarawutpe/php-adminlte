<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /  # Adjust this base path if CodeIgniter is in a subdirectory

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
